# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Este proyecto está en **Expo SDK 54** (`expo: ~54.0.36` en `package.json`) y **React Native 0.81.5**. La documentación de versiones más nuevas propone APIs que acá no existen.
